apiVersion: v1
kind: Pod
metadata:
  name: akane
spec:
  containers:
    - name: akane
      image: ghcr.io/ikanago/akane.go
      envFrom:
        - secretRef:
            name: akane-secret
  nodeSelector:
    nodetype: agent
